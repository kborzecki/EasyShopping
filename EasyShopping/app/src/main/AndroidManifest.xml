<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pk.easyshopping">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">
        <activity android:name=".ShoppingLists" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RecipesList"
            android:parentActivityName=".ShoppingLists"
            android:screenOrientation="portrait">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ShoppingLists" />
        </activity>
        <activity
            android:name=".DetailedRecipe"
            android:parentActivityName=".RecipesList"
            android:screenOrientation="portrait">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".RecipesList" />
        </activity>
        <activity android:name=".ListDetails" android:screenOrientation="portrait"/>
        <activity android:name=".CreateModifyList" android:screenOrientation="portrait"/>
        <activity android:name=".IngredientsList" android:screenOrientation="portrait"></activity>
    </application>

</manifest>