<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pk.easyshopping">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShoppingLists">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RecipesList"
            android:parentActivityName=".ShoppingLists">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ShoppingLists" />
        </activity>
        <activity
            android:name=".DetailedRecipe"
            android:parentActivityName=".RecipesList">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".RecipesList" />
        </activity>
        <activity android:name=".ListDetails" />
        <activity android:name=".CreateModifyList" />
        <activity android:name=".IngredientsList"></activity>
    </application>

</manifest>